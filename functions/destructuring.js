const numbers = ['one', 'two', 'three'];

// without destructuring
const one = numbers[0];
const two = numbers[1];
const three = numbers[2];

// with destructuring
const [first, second, third] = foo;
